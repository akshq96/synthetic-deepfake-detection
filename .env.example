# Copy to .env at the repo root and adjust. See backend/app/core/config.py
# for the full list of settings and their defaults.

# DATABASE_URL=sqlite:///artifacts/app.db
# APP_MLFLOW_TRACKING_URI=sqlite:///artifacts/mlflow.db

# The out-of-the-box default (see backend/app/core/config.py) already routes
# /api/detect to a real pretrained public deepfake classifier, so no
# checkpoint setup is required to try the app. Once you've trained this
# project's own model on a real dataset (e.g. via ml/training/train.py or the
# Synthetic Data Lab), point these at it instead — or leave them unset and
# select your checkpoint per-request via the Detect page's model selector:
# DEFAULT_CHECKPOINT_PATH=/absolute/path/to/artifacts/checkpoints/<run>/best.pt
# DEFAULT_CALIBRATION_PATH=/absolute/path/to/artifacts/checkpoints/<run>/temperature.json
# DEFAULT_MODEL_NAME=efficientnetv2_s
# DEFAULT_IMAGE_SIZE=224
